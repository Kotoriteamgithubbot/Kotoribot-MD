//Login Process
if (loginprocess.includes(m.sender)) {
	if (!usernamesession) {
		if (accountusername.includes(budy)) {
			let configSessionUsername = {
	            id : m.sender
	            username: budy
            }
            usernamesession.push(configSessionUsername)
            await fs.writeFileSync('./database/account/login/usernamesession.json', letChangeJSONToString(usernamesession))
		} else {
			//kirim chat username tidak ditemukan
        }
	} else if (usernamesession && !passwordsession) {
		accountpassword.includes(budy)
    }
}